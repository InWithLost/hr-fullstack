/**
 * Глобальное middleware для логирования навигации между страницами
 * 
 * Это middleware выполняется для ВСЕХ маршрутов (из-за .global в имени файла)
 * и предназначено только для отладки маршрутизации в приложении.
 * Оно не блокирует доступ к страницам и не выполняет никаких проверок.
 */
export default defineNuxtRouteMiddleware((to, from) => {
  // Логирование информации о переходе между страницами
  // Выводит исходный и целевой пути для отладки маршрутизации
  console.log('Navigating from', from.path, 'to', to.path)
})
