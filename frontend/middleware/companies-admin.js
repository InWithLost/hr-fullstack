/**
 * Middleware для страниц управления компаниями в админ-панели
 * 
 * Это middleware не выполняет проверок авторизации в режиме разработки,
 * что позволяет тестировать страницы без необходимости авторизации.
 */
export default defineNuxtRouteMiddleware((to, from) => {
  // Логирование для отладки
  console.log('Companies admin middleware: navigating to', to.path)
  
  // В режиме разработки не выполняем проверки авторизации
  // Это позволяет тестировать страницы без необходимости авторизации
  return
})
